'use client'
import React from "react";
import { inter } from "@/app/ui/fonts";
const Loading: React.FC = (): JSX.Element =>{
    return(
        <div className={`${inter.className} antialiased`}>Loading..</div>
    )
}